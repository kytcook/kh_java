package getlist;

import java.sql.Connection;

public class DeptApp {
	
	void Deptapp() {
		getDeptList() : List<map<String, Object>> // ip번호 포트번호
		
	}
	class.forName(oracle.jdbc.driver.OracleDriver ); // 오라클 밑에 jdbc밑에 드라이버밑에 오라클드라이버 
	//ojdbc.jar 제조사가 안에있다. 벤더
	Connection con = DriverManager.getConnetion(URL, User, pw) // 접속한다. 통로. 채널
	PreparedStatment pstmt = con.prepareStatement(Select, dept)// 전령을 보낸다. Select에서 dept까지 오라클로 보낸다
	ResultSet rs = pstmt.executQuery()	//오라클에게 select문 처리를 맡긴다.
	While(rs.next())
	// while문 안에서는 map을 4번 인스턴스화 해야 한다. 가로가로가로가로
	
	
			
			
	try catch
	public static void main(String[] args) {

		
	}

}
